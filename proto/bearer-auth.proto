syntax = "proto2";

package  bearerauth;

service BearerAuth {
    rpc Check(Request) returns (Response) {}
}

message Request {
    required string message = 1;
}

message Response {
    required string message = 1;
}